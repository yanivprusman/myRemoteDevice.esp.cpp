prompt

help me install:
from the documentation:
FlatBuffers JavaScript library code location
The generated code for the FlatBuffers JavaScript library can be found at https://www.npmjs.com/package/flatbuffers. To use it from sources:
Run npm run compile from the main folder to generate JS files from TS.
In your project, install it as a normal dependency, using the flatbuffers folder as the source.
this is https://www.npmjs.com/package/flatbuffers:

